version: '3.9'
services:
  app:
    container_name: test_app
    build:
      context: ../
      dockerfile: .docker/Dockerfile
    ports:
      - 8080:8080
    restart: unless-stopped
    networks:
      - default
networks:
  default:
    driver: bridge